jQuery ->
  jQuery.noConflict()
  $(".pagination a").live "click", ->
    $("#questions").html('<%= image_tag "loading-machine.gif" %>')
    $.getScript(@href)
    return false <%#prevents browser from performing default action%> 
