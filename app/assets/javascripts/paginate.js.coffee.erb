jQuery ->
  jQuery.noConflict()
  $(".pagination a").live "click", ->
    $("#comments").html('<%=  image_tag "loading-machine.gif" %>')
    $.getScript(@href)
    return false <%#prevents the click operation from performing it original behaviour%> 
